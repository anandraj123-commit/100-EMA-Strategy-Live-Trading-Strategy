import type { PortfolioDocument } from '../../models/Portfolio';
import { getEnvironmentOpenOrders,getEnvironmentPosition } from '../delta';
import { readRuntimeActivity,readStatus } from '../state';
import { hasActivePortfolioTrades } from '../trades/repository';
export type DeletionDependencies={status:(id:string)=>any;activity:(id:string)=>any;hasActiveTrades:(id:string)=>Promise<boolean>;position:(productId:number,environment:'real'|'demo')=>Promise<any>;openOrders:(productId:number,environment:'real'|'demo')=>Promise<any[]>};
const defaults:DeletionDependencies={status:readStatus,activity:readRuntimeActivity,hasActiveTrades:hasActivePortfolioTrades,position:getEnvironmentPosition,openOrders:getEnvironmentOpenOrders};
export async function verifyPortfolioDeletion(portfolio:PortfolioDocument&{_id:NonNullable<PortfolioDocument['_id']>},dependencies:DeletionDependencies=defaults){const id=portfolio._id.toHexString(),status=dependencies.status(id),activity=dependencies.activity(id);if(status.running===true||activity.executionInProgress===true||Number(status.position?.size||0)!==0||status.activeTrade!=null||status.pending!=null||await dependencies.hasActiveTrades(id))return {ok:false as const,reason:'ACTIVE'};let position:any,orders:any[];try{[position,orders]=await Promise.all([dependencies.position(portfolio.productId,portfolio.environment),dependencies.openOrders(portfolio.productId,portfolio.environment)]);}catch{return {ok:false as const,reason:'VERIFICATION_FAILED'};}if(Number(position?.size||0)!==0||orders.length>0)return {ok:false as const,reason:'ACTIVE'};return {ok:true as const};}
