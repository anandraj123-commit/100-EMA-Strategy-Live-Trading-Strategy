import Link from 'next/link';
import { listPortfolio } from '../lib/portfolio/repository';
import { portfolioWithPrices } from '../lib/portfolio/service';
import { getAppMode } from '../lib/app-mode';
export default async function TradeWorkspaceList(){const rows=await portfolioWithPrices(await listPortfolio());return rows.length?<div className="portfolioGrid">{rows.map(row=>{const status=row.runtime??{running:null,trade:'UNAVAILABLE'};return <Link className="portfolioCard workspacePortfolioCard" href={`/futures/dashboard/${row.id}`} key={row.id}><span className="portfolioType">{getAppMode().toUpperCase()}</span><h2>{row.symbol}</h2><dl><div><dt>Current Price</dt><dd>{row.currentPrice??'Unavailable'}</dd></div><div><dt>Robot Status</dt><dd>{status.running===null?'UNAVAILABLE':status.running?'RUNNING':'STOPPED'}</dd></div><div><dt>Position Status</dt><dd>{status.trade}</dd></div></dl></Link>;})}</div>:<section className="portfolioEmpty"><h2>No {getAppMode().toUpperCase()} portfolios.</h2><p>Add an instrument from Portfolio first.</p><Link href="/futures/portfolio">Open Portfolio</Link></section>;}
